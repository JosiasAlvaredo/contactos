import React from 'react';
import '@picocss/pico';

function Header() {
    return (
        <nav className="nav">
            <img src=".assets/logo.png" alt="Logo" />
            <ul>
            </ul>
        </nav>
    );
}

export default Header;